# MCP Team Memory Server

Self-hosted платформа долгосрочной памяти для AI-ассистентов (Claude Code, Cursor, GPT) с поддержкой множества проектов и пользователей.

## Что это?

**MCP Team Memory** позволяет AI-ассистентам помнить информацию между сессиями. Вместо того чтобы каждый раз объяснять контекст проекта, предпочтения, решённые проблемы — ассистент запоминает это навсегда.

---

## 0. Компоненты системы

### Инфраструктурные сервисы

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **PostgreSQL + pgvector** | pgvector/pgvector:pg17 | SSOT хранилище, векторный поиск |
| **Neo4j** | neo4j:5.26-community | Граф связей для Mem0 |
| **FalkorDB** | falkordb/falkordb:latest | Temporal graph для Graphiti |
| **Ollama** | ollama/ollama | Локальные embeddings (nomic-embed-text) |
| **Nginx** | nginx:latest | API Gateway, роутинг, авторизация |

### Per-Project сервисы

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **unified-{project}** | FastMCP (Python) | Unified MCP Server — единое окно для всех инструментов |
| **mem0-{project}** | Mem0 + Anthropic | Автоматическая экстракция фактов из разговоров |

### Shared сервисы

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Graphiti** | graphiti-core | Temporal Knowledge Graph с LLM экстракцией |

### LLM провайдеры

| Задача | Провайдер | Модель |
|--------|-----------|--------|
| Экстракция фактов (Mem0) | Anthropic | claude-3-haiku |
| Экстракция сущностей (Graphiti) | Anthropic | claude-3-haiku |
| Embeddings | Ollama (local) | nomic-embed-text |

---

## 1. Типы памяти и их назначение

### 🧠 System 1 Memory (Быстрые факты)
**Mem0 + SSOT PostgreSQL**

Автоматически извлекаемые факты из разговоров. Используется для:
- Предпочтения разработчика ("использует TypeScript", "любит функциональный стиль")
- Контекст текущих задач ("работает над модулем авторизации")
- Технические решения ("выбрали JWT для токенов")

```python
# Добавление с 5-уровневой дедупликацией
ssot_memory_add(
    content="Используем PostgreSQL RLS для изоляции тенантов",
    user_id="dev1",
    memory_type="system1",
    domain="architecture"
)

# Поиск
ssot_memory_search(query="изоляция данных", limit=5)
```

### 🔬 System 2 Memory (Reasoning Traces)
**SSOT PostgreSQL**

Сложные рассуждения и цепочки мыслей. Используется для:
- Архитектурные решения с обоснованием
- Debugging sessions
- Пошаговые решения проблем

```python
ssot_memory_add(
    content="Выбрали RLS вместо application-level filtering потому что: 1) безопаснее, 2) проще поддерживать",
    memory_type="system2",
    domain="security"
)
```

### 👥 Workspace Memory (Командный контекст)
**SSOT PostgreSQL**

Командная память — кто над чем работает. Используется для:
- Статус задач членов команды
- Активности в проекте
- Координация работы

```python
ssot_workspace_add(
    content="Завершена интеграция SSOT с дедупликацией",
    team_member="claude",
    activity_type="development",
    status="completed"
)
```

### 📚 Knowledge Base (Документация)
**FAISS + Ollama embeddings**

Семантический поиск по документации проекта. Используется для:
- Архитектурная документация
- API спецификации
- Руководства и README

```python
# Создание базы
kb_create(
    name="architecture",
    documents=["# Auth Module\nJWT tokens stored in cookies..."]
)

# Поиск
kb_search(name="architecture", query="как работает авторизация")
```

### 🔗 Graphiti (Temporal Knowledge Graph)
**FalkorDB + Anthropic LLM**

Граф сущностей и связей с историей изменений. Используется для:
- Зависимости между компонентами
- Эволюция архитектуры
- Связи между концепциями

```python
graphiti_add_episode(
    content="Компонент AuthService зависит от JWTValidator и UserRepository",
    name="architecture-deps"
)

graphiti_search_facts(query="зависимости AuthService")
```

---

## 2. Концепция SSOT (Single Source of Truth)

### Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                    SSOT Architecture                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              PostgreSQL + pgvector (SSOT)                 │  │
│  │                                                           │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │  │
│  │  │  memories   │  │  workspace  │  │  dedup_registry │   │  │
│  │  │  (System1/2)│  │  _memories  │  │  (5-layer)      │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘   │  │
│  │                                                           │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │  │
│  │  │  graphiti_  │  │  graphiti_  │  │  kb_chunks      │   │  │
│  │  │  episodes   │  │  entities   │  │  (documentation)│   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘   │  │
│  │                                                           │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │           unified_search_view                        │ │  │
│  │  │   (поиск по ВСЕМ источникам одним запросом)         │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ▲                                  │
│                              │                                  │
│  ┌───────────────────────────┼──────────────────────────────┐  │
│  │     DeduplicationOrchestrator (5-Layer Cipher)           │  │
│  │                                                           │  │
│  │  Layer 1: Significance Filter (>20 chars, not trivial)   │  │
│  │  Layer 2: Hash Check (exact duplicate)                    │  │
│  │  Layer 3: Embedding Similarity (semantic match)           │  │
│  │  Layer 4: High Similarity Gate (>0.9 = duplicate)         │  │
│  │  Layer 5: Cross-System Check (all sources)                │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              ▲                                  │
│                              │                                  │
│  ┌───────────────────────────┼──────────────────────────────┐  │
│  │              Unified MCP Server                           │  │
│  │                                                           │  │
│  │   47 инструментов через единое окно                       │  │
│  │   - mem0_* (25 tools) - память                           │  │
│  │   - kb_* (6 tools) - knowledge base                       │  │
│  │   - graphiti_* (10 tools) - temporal graph                │  │
│  │   - ssot_* (6 tools) - SSOT layer                         │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Преимущества SSOT

1. **Единый поиск** — `ssot_unified_search` ищет по ВСЕМ источникам
2. **Дедупликация** — 5-уровневая фильтрация дубликатов (Cipher approach)
3. **Консистентность** — все данные в одной БД
4. **Простота бэкапа** — один PostgreSQL dump
5. **Масштабируемость** — pgvector для миллионов записей

---

## 3. Мультипроекты и мультипользователи

### Изоляция данных

```
┌─────────────────────────────────────────────────────────────────┐
│                       Data Isolation                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   PostgreSQL SSOT:  Per-project database                        │
│   ├── mem0_terra        ← изолированная БД                      │
│   ├── mem0_sport        ← изолированная БД                      │
│   ├── mem0_datashowcase ← изолированная БД                      │
│   └── mem0_trialprj     ← изолированная БД                      │
│                                                                  │
│   Memory (Mem0):    Per-project + Per-user                      │
│   ├── terra/user_alice  ← изолировано по user_id                │
│   ├── terra/user_bob    ← изолировано по user_id                │
│   └── sport/user_coach  ← изолировано по user_id                │
│                                                                  │
│   Knowledge Base:   Per-project (shared within project)         │
│   ├── terra KB          ← общая для всех user в terra           │
│   └── sport KB          ← общая для всех user в sport           │
│                                                                  │
│   Graphiti:         Per-project via group_id                    │
│   └── Single instance, group_id = project_name                  │
│                                                                  │
│   Workspace Memory: Per-project (team context)                  │
│   └── Командный контекст общий для проекта                      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Текущие проекты

| Проект | Endpoint | Пользователи | База данных |
|--------|----------|--------------|-------------|
| **terra** | `/terra/mcp?key=...` | user00-user20 | mem0_terra |
| **sport** | `/sport/mcp?key=...` | user00-user20 | mem0_sport |
| **datashowcase** | `/datashowcase/mcp?key=...` | user00-user20 | mem0_datashowcase |
| **trialprj** | `/trialprj/mcp?key=...` | user00-user20 | mem0_trialprj |

### Добавление нового проекта

```bash
# 1. Создать базу данных
docker exec mcp-postgres psql -U mem0 -d postgres -c "CREATE DATABASE mem0_newproject;"
docker exec mcp-postgres psql -U mem0 -d mem0_newproject -c "CREATE EXTENSION IF NOT EXISTS vector;"

# 2. Применить SSOT схему
docker exec mcp-postgres psql -U mem0 -d mem0_newproject -f /docker-entrypoint-initdb.d/ssot_schema.sql

# 3. Сгенерировать API ключи
./scripts/generate-keys.sh newproject

# 4. Добавить в docker-compose.yml (секции mem0-newproject, unified-newproject)

# 5. Добавить роутинг в nginx.conf

# 6. Перезапустить
docker compose up -d --build
docker exec mcp-nginx nginx -s reload
```

### Добавление пользователя в проект

```bash
# Добавить ключ в nginx/api-keys-{project}.conf
echo '"newproject_newuser_45dd5fbf816adb64a30ca52ab20704b6" 1;  # newuser' >> nginx/api-keys-newproject.conf

# Применить
docker exec mcp-nginx nginx -s reload
```

---

## 4. Полный список MCP инструментов (47 tools)

### SSOT Layer (6 tools) — Рекомендуется использовать

| Инструмент | Описание |
|------------|----------|
| `ssot_memory_add` | Добавить память с 5-уровневой дедупликацией. Возвращает ADD/UPDATE/NONE |
| `ssot_memory_search` | Семантический поиск по памяти с similarity score |
| `ssot_unified_search` | Поиск по ВСЕМ источникам (memories, episodes, entities, KB) |
| `ssot_workspace_add` | Добавить командный контекст (кто/что/статус) |
| `ssot_sync_episode` | Синхронизировать эпизод из Graphiti в SSOT |
| `ssot_get_stats` | Статистика по всем источникам данных |

### Memory — Mem0 (25 tools)

| Инструмент | Описание |
|------------|----------|
| `mem0_add` | Добавить память (без дедупликации) |
| `mem0_search` | Семантический поиск |
| `mem0_get_all` | Получить все записи пользователя |
| `mem0_get` | Получить запись по ID |
| `mem0_update` | Обновить запись |
| `mem0_delete` | Удалить запись |
| `mem0_delete_all` | Удалить все записи пользователя |
| `mem0_history` | История изменений записи |
| `mem0_enhanced_search` | Поиск с графовым контекстом |
| `mem0_graph_sync` | Синхронизация с Neo4j |
| `mem0_graph_link` | Связать две записи |
| `mem0_graph_related` | Найти связанные записи |
| `mem0_graph_path` | Путь между записями в графе |
| `mem0_graph_evolution` | Эволюция знаний по теме |
| `mem0_graph_superseded` | Устаревшие записи |
| `mem0_graph_thread` | Тред разговора для записи |
| `mem0_graph_communities` | Кластеры записей |
| `mem0_graph_trust_score` | Оценка доверия к записи |
| `mem0_graph_intelligence` | Аналитика по памяти |
| `mem0_create_decision` | Создать запись решения |
| `mem0_get_decision` | Получить решение с обоснованием |
| `mem0_create_component` | Создать архитектурный компонент |
| `mem0_component_dependency` | Связать зависимости компонентов |
| `mem0_component_link_memory` | Связать память с компонентом |
| `mem0_component_impact` | Анализ влияния изменения компонента |

### Knowledge Base (6 tools)

| Инструмент | Описание |
|------------|----------|
| `kb_create` | Создать KB из документов (JSON array) |
| `kb_search` | Семантический поиск по KB |
| `kb_list` | Список всех KB |
| `kb_delete` | Удалить KB |
| `kb_add_documents` | Добавить документы в KB |
| `kb_stats` | Статистика KB |

### Graphiti — Temporal Knowledge Graph (10 tools)

| Инструмент | Описание |
|------------|----------|
| `graphiti_add_episode` | Добавить эпизод (LLM экстрагирует сущности) |
| `graphiti_search_nodes` | Поиск узлов (сущностей) |
| `graphiti_search_facts` | Поиск фактов (связей) |
| `graphiti_get_episodes` | Последние эпизоды |
| `graphiti_delete_episode` | Удалить эпизод |
| `graphiti_get_entity_edge` | Получить связь по UUID |
| `graphiti_delete_entity_edge` | Удалить связь |
| `graphiti_get_status` | Статус Graphiti |
| `graphiti_clear_graph` | Очистить граф проекта |
| `get_project_info` | Информация о проекте и доступных features |

---

## 5. Автоматическое обновление и чтение данных

### Рекомендуемый workflow для Claude Code

```markdown
# В CLAUDE.md проекта добавить:

## MCP Memory (Долгосрочная память)

**⚠️ ПЕРВЫМ ДЕЛОМ в каждой новой сессии:**
1. Вызови `ssot_unified_search(query="текущий контекст проекта")`
2. Вызови `ssot_get_stats()` для понимания объёма данных

**После каждой значимой работы:**
1. Сохрани контекст: `ssot_memory_add(content="что сделано", memory_type="system1")`
2. Для решений: `ssot_memory_add(content="решение + обоснование", memory_type="system2")`
3. Для командной работы: `ssot_workspace_add(content="статус", team_member="claude")`

**При исследовании кодовой базы:**
1. Найденные паттерны → `ssot_memory_add(domain="patterns")`
2. Архитектурные решения → `graphiti_add_episode()`
3. Документацию → `kb_create()` или `kb_add_documents()`
```

### Автоматическая дедупликация

При использовании `ssot_memory_add` система автоматически:

1. **Проверяет значимость** — отсеивает тривиальные записи (<20 символов)
2. **Сравнивает хеш** — не дублирует точные копии
3. **Сравнивает семантику** — находит похожие записи (embedding similarity)
4. **Блокирует дубликаты** — если similarity > 0.9, возвращает NONE
5. **Обновляет существующие** — при частичном совпадении может обновить

```python
# Первый вызов
ssot_memory_add(content="Используем PostgreSQL RLS")
# → {"status": "added", "operation": "ADD"}

# Повторный вызов (точная копия)
ssot_memory_add(content="Используем PostgreSQL RLS")
# → {"status": "skipped", "reason": "exact_hash", "similarity": 1.0}

# Похожий контент
ssot_memory_add(content="PostgreSQL RLS используется для изоляции")
# → {"status": "added", "operation": "ADD"} (similarity < 0.9)
```

---

## Быстрый старт

### Подключение к Claude Code

Создайте `.mcp.json` в корне вашего проекта:

```json
{
  "mcpServers": {
    "team-memory": {
      "type": "sse",
      "url": "http://46.149.66.107/datashowcase/mcp?key=datashowcase_user00_9a2a97e56581f3708dbc14999ae03b48"
    }
  }
}
```

### Проверка подключения

```bash
# Health check
curl "http://46.149.66.107/datashowcase/mcp?key=..." \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'
```

---

## Инфраструктура

### Ресурсы

- **Текущее использование:** ~4GB RAM для 4 проектов
- **Доступно:** 11GB RAM
- **Ёмкость:** ~15-20 проектов

### Контейнеры

| Container | Purpose | Memory |
|-----------|---------|--------|
| mcp-nginx | API Gateway + Auth | 64MB |
| unified-{project} | Unified MCP Server | 256MB per project |
| mem0-{project} | Memory extraction | 512MB per project |
| graphiti | Knowledge graph (shared) | 768MB |
| mcp-postgres | PostgreSQL + pgvector | 512MB |
| mcp-neo4j | Neo4j for Mem0 | 1GB |
| mcp-falkordb | FalkorDB for Graphiti | 1GB |
| mcp-ollama | Embeddings (nomic-embed-text) | 4GB |

---

## Troubleshooting

### SSOT не инициализируется

```bash
# Проверить логи
docker logs unified-{project} --tail 50 | grep -i ssot

# Должно быть:
# PostgreSQL SSOT initialized for project {project}
# SSOT layer initialized with 5-layer deduplication
# Server ready: mem0=True, kb=True, graphiti=True, ssot=True
```

### Дедупликация не работает

```bash
# Проверить dedup_registry
docker exec mcp-postgres psql -U mem0 -d mem0_{project} -c "SELECT count(*) FROM dedup_registry;"

# Проверить embedding generation
docker logs mcp-ollama --tail 10
```

### unified_search возвращает пусто

```bash
# Проверить статистику
curl ... -d '{"method":"tools/call","params":{"name":"ssot_get_stats","arguments":{}}}'

# Проверить что данные есть
docker exec mcp-postgres psql -U mem0 -d mem0_{project} -c "SELECT source_type, count(*) FROM unified_search_view GROUP BY source_type;"
```

---

## Changelog

### v2.0.0 (2025-12-06) — SSOT Architecture

- ✅ PostgreSQL SSOT layer с 9 таблицами
- ✅ 5-уровневая дедупликация (Cipher approach)
- ✅ 6 новых SSOT инструментов
- ✅ Unified search view
- ✅ Workspace memory для командной работы
- ✅ System 1 / System 2 memory types

### v1.0.0 (2025-12-05) — Initial Release

- ✅ Mem0 + Anthropic Claude
- ✅ Graphiti temporal graph
- ✅ Knowledge Base с FAISS
- ✅ Мультипроекты и мультипользователи
- ✅ SSE transport для Claude Code

---

## Лицензия

MIT
