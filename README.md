# MCP Team Memory Server

Self-hosted Memory + Knowledge Base для команд разработки с мульти-проектной поддержкой.

## Возможности

### 🧠 Долгосрочная память
- **Персональная память** — каждый разработчик имеет свой контекст
- **Командная память** — общие знания доступны всей команде проекта
- **Автоматическая категоризация** — память структурируется автоматически

### 🔗 Knowledge Graph
- **Связи между знаниями** — Neo4j хранит отношения между фактами
- **Семантический поиск** — поиск по смыслу, не только по ключевым словам
- **Векторные embeddings** — PostgreSQL + pgvector для быстрого similarity search

### 🏢 Мульти-проектность
- **Изоляция проектов** — каждый проект имеет отдельную память
- **API ключи** — уникальный ключ для каждого пользователя
- **Единый endpoint** — один сервер для всех проектов

### 🔒 Приватность и безопасность
- **Self-hosted** — данные остаются на вашем сервере
- **Локальные LLM** — Ollama (qwen3:8b + nomic-embed-text), без OpenAI
- **API key аутентификация** — защита доступа к памяти

### 🚀 Простая интеграция
- **MCP протокол** — нативная поддержка в Claude Desktop/Code, Cursor
- **HTTP Stream transport** — рекомендуемый современный транспорт
- **SSE transport** — legacy поддержка
- **Единый URL** — ключ передаётся в параметре URL

## Технологии

- **Mem0 MCP Server** — память и Knowledge Graph
- **PostgreSQL + pgvector** — векторный поиск
- **Neo4j** — Knowledge Graph
- **Ollama** — локальные LLM и embeddings
- **Nginx** — reverse proxy с API key валидацией

## Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone https://github.com/erofeev/xmr-mem-0.git
cd xmr-mem-0

# 2. Создать .env с паролями
cp .env.example .env

# 3. Создать API ключи для проектов
# nginx/api-keys-PROJECT.conf
echo 'project_abc123... 1;' >> nginx/api-keys-myproject.conf

# 4. Запустить
docker compose up -d

# 5. Проверить
curl http://SERVER_IP/health
```

## Конфигурация клиента

### HTTP Stream (рекомендуется)

```json
{
  mcpServers: {
    team-memory: {
      url: http://SERVER_IP/PROJECT/mcp/?key=YOUR_API_KEY,
      transport: http
    }
  }
}
```

### SSE (legacy)

```json
{
  mcpServers: {
    team-memory: {
      url: http://SERVER_IP/PROJECT/sse/?key=YOUR_API_KEY,
      transport: sse
    }
  }
}
```

### Claude CLI

```bash
# HTTP Stream (рекомендуется)
claude mcp add --transport http team-memory http://SERVER_IP/PROJECT/mcp/?key=YOUR_API_KEY

# SSE (legacy)
claude mcp add --transport sse team-memory http://SERVER_IP/PROJECT/sse/?key=YOUR_API_KEY
```

## API Endpoints

| Endpoint | Описание |
|----------|----------|
| `POST /PROJECT/mcp/?key=API_KEY` | HTTP Stream endpoint (рекомендуется) |
| `GET /PROJECT/sse/?key=API_KEY` | SSE соединение (legacy) |
| `POST /PROJECT/sse/messages/?session_id=XXX` | SSE сообщения |
| `GET /health` | Health check |

## MCP инструменты

### Core Memory Tools
- `add_coding_preference` — добавить код/знания в память
- `search_coding_preferences` — семантический поиск
- `get_all_coding_preferences` — получить все записи
- `delete_memory` — удалить память
- `get_memory_history` — история изменений

### Memory Intelligence Tools
- `link_memories` — создать связи между записями
- `get_related_memories` — найти связанные знания
- `analyze_memory_intelligence` — комплексный анализ памяти
- `create_component` — создать компонент архитектуры
- `link_component_dependency` — связать компоненты
- `analyze_component_impact` — анализ влияния изменений
- `create_decision` — зафиксировать решение
- `get_decision_rationale` — получить контекст решения

## Архитектура

```
                    ┌─────────────────────────────────────┐
                    │            Nginx                    │
                    │  (API key validation + routing)     │
                    └──────────────┬──────────────────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                    │
              ▼                    ▼                    ▼
    ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
    │   MCP Terra     │  │   MCP Sport     │  │   MCP ...       │
    │   (port 8080)   │  │   (port 8080)   │  │   (port 8080)   │
    └────────┬────────┘  └────────┬────────┘  └────────┬────────┘
             │                    │                    │
             ▼                    ▼                    ▼
    ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
    │   Mem0 Terra    │  │   Mem0 Sport    │  │   Mem0 ...      │
    │   (port 8000)   │  │   (port 8000)   │  │   (port 8000)   │
    └────────┬────────┘  └────────┬────────┘  └────────┬────────┘
             │                    │                    │
             └────────────────────┼────────────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              │                   │                   │
              ▼                   ▼                   ▼
    ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
    │   PostgreSQL    │  │     Neo4j       │  │    Ollama       │
    │   + pgvector    │  │  (знания граф)  │  │  (embeddings)   │
    └─────────────────┘  └─────────────────┘  └─────────────────┘
```

## Требования

- Docker + Docker Compose
- 8+ GB RAM (рекомендуется 12 GB)
- 20+ GB диск

## Стоимость хостинга

~2000 руб/мес (Timeweb Cloud, 12GB RAM, 4 vCPU)
