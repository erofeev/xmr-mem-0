# MCP Team Memory Server

Self-hosted Memory + Knowledge Base для команд разработки с мульти-проектной поддержкой.

## Возможности

### 🧠 Долгосрочная память
- **Персональная память** — каждый разработчик имеет свой контекст
- **Командная память** — общие знания доступны всей команде проекта
- **Автоматическая категоризация** — память структурируется автоматически

### 🔗 Knowledge Graph
- **Связи между знаниями** — Neo4j хранит отношения между фактами
- **Семантический поиск** — поиск по смыслу, не только по ключевым словам
- **Векторные embeddings** — PostgreSQL + pgvector для быстрого similarity search

### 🏢 Мульти-проектность
- **Изоляция проектов** — каждый проект имеет отдельную память
- **API ключи** — уникальный ключ для каждого пользователя
- **Единый endpoint** — один сервер для всех проектов

### 🔒 Приватность и безопасность
- **Self-hosted** — данные остаются на вашем сервере
- **Локальные LLM** — Ollama (qwen3:8b + nomic-embed-text), без OpenAI
- **API key аутентификация** — защита доступа к памяти

### 🚀 Простая интеграция
- **MCP протокол** — нативная поддержка в Claude Desktop/Code, Cursor
- **SSE transport** — real-time коммуникация
- **Единый URL** — ключ передаётся в параметре URL

## Технологии

- **Mem0 MCP Server** — память и Knowledge Graph
- **PostgreSQL + pgvector** — векторный поиск
- **Neo4j** — Knowledge Graph
- **Ollama** — локальные LLM и embeddings
- **MCP Gateway** — Express прокси с роутингом по проектам
- **Nginx** — reverse proxy

## Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone https://github.com/erofeev/xmr-mem-0.git
cd xmr-mem-0

# 2. Создать .env с паролями
cp .env.example .env

# 3. Создать API ключи для проектов
# nginx/api-keys-PROJECT.conf
echo "\"project_abc123...\" 1;" >> nginx/api-keys-myproject.conf

# 4. Запустить
docker compose up -d

# 5. Проверить
curl http://SERVER_IP/health
```

## Конфигурация клиента

```json
{
  "mcpServers": {
    "team-memory": {
      "url": "http://SERVER_IP/mcp/sse/?key=YOUR_API_KEY",
      "transport": "sse"
    }
  }
}
```

## API Endpoints

| Endpoint | Описание |
|----------|----------|
| `GET /mcp/sse/?key=API_KEY` | SSE соединение для MCP |
| `POST /mcp/sse/messages/?session_id=XXX` | Отправка сообщений |
| `GET /health` | Health check |

## MCP инструменты

- `add_memory` — добавить память
- `search_memory` — поиск в памяти  
- `get_all_memories` — получить все записи
- `delete_memory` — удалить память

## Требования

- Docker + Docker Compose
- 8+ GB RAM (рекомендуется 12 GB)
- 20+ GB диск

## Стоимость хостинга

~2000 руб/мес (Timeweb Cloud, 12GB RAM, 4 vCPU)
