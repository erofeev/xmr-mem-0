# MCP Team Memory Server

Self-hosted платформа долгосрочной памяти для AI-ассистентов (Claude, GPT, Cursor) с поддержкой множества проектов и пользователей.

## Что это?

**MCP Team Memory** позволяет AI-ассистентам помнить информацию между сессиями. Вместо того чтобы каждый раз объяснять контекст проекта, предпочтения, решённые проблемы — ассистент запоминает это навсегда.

### Три компонента платформы

| Компонент | Что делает | Для чего использовать |
|-----------|------------|----------------------|
| **Memory (Mem0)** | Автоматически извлекает факты из разговора | Личные предпочтения, контекст задач |
| **Knowledge Base** | Хранит документы с семантическим поиском | Документация проекта, архитектура |
| **Cognee** | Строит граф знаний (сущности + связи) | Зависимости, архитектурные связи |

---

## Мультипроектность и мультипользовательность

### Как это работает

```
┌─────────────────────────────────────────────────────────────────┐
│                         MCP Server                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   Project: TERRA                    Project: SPORT               │
│   API Key: terra_xxx                API Key: sport_xxx           │
│   ┌─────────────────────┐           ┌─────────────────────┐     │
│   │ 🔒 Memory (private) │           │ 🔒 Memory (private) │     │
│   │  ├── user: alice    │           │  ├── user: coach    │     │
│   │  ├── user: bob      │           │  └── user: analyst  │     │
│   │  └── user: default  │           │                     │     │
│   ├─────────────────────┤           ├─────────────────────┤     │
│   │ 🌐 KB (shared)      │           │ 🌐 KB (shared)      │     │
│   │  └── architecture/  │           │  └── rules/         │     │
│   ├─────────────────────┤           ├─────────────────────┤     │
│   │ 🌐 Cognee (shared)  │           │ 🌐 Cognee (shared)  │     │
│   │  └── graph: terra   │           │  └── graph: sport   │     │
│   └─────────────────────┘           └─────────────────────┘     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Уровни изоляции

| Уровень | Что изолировано | Как разделяется |
|---------|-----------------|-----------------|
| **Project** | Всё | Разные API ключи, разные данные |
| **User** | Только Memory | Параметр `user_id` в вызовах |
| **Shared** | KB + Cognee | Общие для всех в проекте |

### Примеры

**Два проекта на одном сервере:**
```
http://server/terra/mcp/?key=terra_abc123  → Project TERRA
http://server/sport/mcp/?key=sport_xyz789  → Project SPORT
```

**Два разработчика в одном проекте:**
```python
# Alice сохраняет своё
memory_add(content="Предпочитаю Python", user_id="alice")

# Bob сохраняет своё  
memory_add(content="Предпочитаю Go", user_id="bob")

# Alice ищет — видит только своё
memory_search(query="язык", user_id="alice")  # → Python

# Bob ищет — видит только своё
memory_search(query="язык", user_id="bob")    # → Go
```

**Общая база знаний:**
```python
# Alice добавляет документ
kb_add(knowledge_base="arch", filename="api.md", content="...")

# Bob может найти этот документ
kb_search(knowledge_base="arch", query="API")  # → Найдёт!
```

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                          VPS Server                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐                                                │
│  │   Nginx     │  /{project}/mcp/?key=API_KEY                   │
│  │  (порт 80)  │  Проверка ключа + маршрутизация                │
│  └──────┬──────┘                                                │
│         │                                                       │
│         ▼                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           Unified MCP Server (FastMCP)                   │   │
│  │                    порт 8080                             │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │   │
│  │  │  Memory  │  │    KB    │  │  Cognee  │              │   │
│  │  │  Tools   │  │  Tools   │  │  Tools   │              │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘              │   │
│  └───────┼─────────────┼─────────────┼──────────────────────┘   │
│          │             │             │                          │
│          ▼             ▼             ▼                          │
│  ┌─────────────┐  ┌─────────┐  ┌─────────────┐                 │
│  │   Mem0 API  │  │  FAISS  │  │   Neo4j     │                 │
│  │  + Qdrant   │  │  Index  │  │  Graph DB   │                 │
│  └──────┬──────┘  └─────────┘  └─────────────┘                 │
│         │                                                       │
│         ▼                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Ollama (LLM)                          │   │
│  │   • qwen3:8b - экстракция фактов, Cognee анализ         │   │
│  │   • nomic-embed-text - эмбеддинги для поиска            │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Как Claude использует Memory для экономии токенов

### Проблема без Memory

```
Session 1:
User: "Я работаю над проектом Data Showcase, использую Moleculer.js..."
      (500 токенов объяснения контекста)

Session 2:
User: "Напомню, проект Data Showcase, Moleculer.js, PostgreSQL с RLS..."
      (500 токенов снова)

Session 3:
User: "Итак, как я говорил, Data Showcase это..."
      (500 токенов каждый раз!)
```

**Итого: 1500+ токенов потрачено на повторение контекста**

### Решение с Memory

```
Session 1:
User: "Я работаю над проектом Data Showcase..."
Claude: [вызывает memory_add автоматически]
        memory_add(content="Разработчик работает над Data Showcase, 
                           использует Moleculer.js, PostgreSQL с RLS",
                  user_id="dev1")

Session 2:
Claude: [в начале сессии вызывает memory_search]
        memory_search(query="контекст проекта", user_id="dev1")
        → "Data Showcase, Moleculer.js, PostgreSQL с RLS"
        
User: "Добавь новый endpoint"
Claude: [уже знает контекст, не нужно объяснять]
```

**Итого: 50 токенов на memory_search вместо 500 на объяснение**

### Практические сценарии экономии

| Сценарий | Без Memory | С Memory | Экономия |
|----------|------------|----------|----------|
| Контекст проекта | 500 токенов/сессия | 50 токенов | 90% |
| Предпочтения стиля | 200 токенов | 30 токенов | 85% |
| Решённые баги | 1000 токенов | 100 токенов | 90% |
| Архитектура (KB) | 2000 токенов | 200 токенов | 90% |

---

## Быстрый старт

### 1. Клонирование

```bash
git clone https://github.com/your-repo/mcp-team-memory.git
cd mcp-team-memory
cp .env.example .env
```

### 2. Запуск

```bash
docker compose up -d
```

### 3. Подключение Claude Code

`.mcp.json` в корне проекта:
```json
{
  "mcpServers": {
    "team-memory": {
      "type": "http",
      "url": "http://YOUR_SERVER/{project}/mcp/?key=YOUR_API_KEY"
    }
  }
}
```

---

## Детальное описание компонентов

### 1. Memory (Mem0) — Долгосрочная память

#### Что это даёт разработчику

| Фича | Как работает | Пример |
|------|--------------|--------|
| **Автоизвлечение фактов** | LLM анализирует текст и выделяет факты | "Я люблю TypeScript" → сохранит факт о предпочтении |
| **Семантический поиск** | Находит по смыслу, не по словам | Запрос "язык" найдёт "TypeScript" |
| **User isolation** | Каждый видит только свои записи | alice не видит записи bob |
| **Дедупликация** | Не создаёт дубликаты | Повторный факт обновит старый |
| **Метаданные** | JSON теги для категоризации | `{"type": "bugfix", "module": "auth"}` |
| **Автообновление** | Новая информация дополняет старую | "Теперь использую Neovim" обновит запись о редакторе |

#### Инструменты

```
memory_add(content, user_id?, metadata?)     — Добавить запись
memory_search(query, user_id?, limit?)       — Семантический поиск
memory_get_all(user_id?, limit?)             — Все записи пользователя
memory_delete(memory_id)                     — Удалить запись
memory_get_users()                           — Список пользователей
```

#### Примеры использования

```python
# 1. Сохранить предпочтения разработчика
memory_add(
    content="Разработчик использует Neovim с TypeScript LSP, предпочитает функциональный стиль",
    user_id="alexey"
)

# 2. Сохранить решённый баг
memory_add(
    content="Баг: RLS не работал. Причина: user_id не передавался в ctx.meta. Фикс: добавить в middleware",
    user_id="team",
    metadata='{"type": "bugfix", "module": "data-access"}'
)

# 3. Найти контекст в новой сессии
memory_search(query="над чем работает разработчик", user_id="alexey")

# 4. Найти решения похожих багов
memory_search(query="проблемы с RLS", user_id="team")
```

---

### 2. Knowledge Base — База знаний проекта

#### Что это даёт разработчику

| Фича | Как работает | Пример |
|------|--------------|--------|
| **Markdown документы** | Загружаете .md файлы | README, ADR, архитектура |
| **Chunking** | Разбивает на части (500 символов + overlap) | Большой документ → много чанков |
| **Семантический поиск** | FAISS + эмбеддинги | "авторизация" найдёт про JWT |
| **Множество баз** | Разные KB для разных целей | `architecture/`, `api-docs/`, `tutorials/` |
| **Быстрый индекс** | FAISS = миллисекунды на поиск | Даже 1000 документов — мгновенно |
| **Shared доступ** | Вся команда видит все документы | Один добавил — все ищут |

#### Инструменты

```
kb_add(knowledge_base, filename, content)    — Добавить документ
kb_search(knowledge_base, query, limit?)     — Поиск по KB
kb_list()                                    — Список баз знаний
kb_stats(knowledge_base)                     — Статистика
kb_reindex(knowledge_base)                   — Пересоздать индекс
```

#### Примеры использования

```python
# 1. Добавить архитектурное решение
kb_add(
    knowledge_base="architecture",
    filename="adr-001-rls.md",
    content="""
    # ADR-001: Row Level Security
    
    ## Контекст
    Нужна изоляция данных между тенантами.
    
    ## Решение
    PostgreSQL RLS policies на уровне БД.
    
    ## Последствия
    - Безопасность гарантируется БД
    - Невозможно забыть фильтр в коде
    """
)

# 2. Новый разработчик ищет информацию
kb_search(
    knowledge_base="architecture",
    query="как изолируются данные тенантов",
    limit=3
)
# → Вернёт ADR-001-rls.md с релевантными чанками

# 3. Посмотреть статистику
kb_stats(knowledge_base="architecture")
# → {"documents": 15, "chunks": 127, "index_size": "2.3 MB"}
```

---

### 3. Cognee — Граф знаний (Knowledge Graph)

#### Что это даёт разработчику

| Фича | Как работает | Пример |
|------|--------------|--------|
| **Экстракция сущностей** | LLM находит: люди, технологии, концепции | "Moleculer.js" → Entity(type=Technology) |
| **Связи между сущностями** | Автоматически определяет отношения | API Gateway → "uses" → Moleculer.js |
| **Neo4j хранение** | Настоящая графовая БД | Cypher запросы, traversal |
| **Insights поиск** | Высокоуровневые выводы | "Что сломается если упадёт Redis?" |
| **Graph completion** | Предлагает недостающие связи | "Возможно, X также зависит от Y" |
| **Типы сущностей** | Person, Technology, Concept, Service | Фильтрация по типу |

#### Инструменты

```
cognee_add(content, source?)                 — Добавить в граф
cognee_search(query, search_type?)           — Поиск (insights/chunks/graph_completion)
cognee_graph_stats()                         — Статистика графа
cognee_get_entities(entity_type?, limit?)    — Получить сущности
cognee_get_relationships(entity_id?, limit?) — Получить связи
```

#### Примеры использования

```python
# 1. Загрузить описание архитектуры
cognee_add(
    content="""
    Data Showcase использует микросервисную архитектуру на Moleculer.js.
    API Gateway принимает HTTP запросы и маршрутизирует к сервисам.
    Sites Service зависит от Data Access Layer.
    Data Access использует PostgreSQL с Row Level Security.
    Кеширование реализовано через Redis.
    Сообщения между сервисами идут через NATS.
    """,
    source="architecture"
)

# 2. Найти зависимости
cognee_search(
    query="что зависит от PostgreSQL",
    search_type="insights"
)
# → "Data Access Layer напрямую, Sites Service транзитивно..."

# 3. Получить все технологии
cognee_get_entities(entity_type="Technology")
# → [Moleculer.js, PostgreSQL, Redis, NATS, Angular...]

# 4. Посмотреть связи компонента
cognee_get_relationships(entity_id="api-gateway")
# → [
#     {from: "API Gateway", type: "routes_to", to: "Sites Service"},
#     {from: "API Gateway", type: "uses", to: "Moleculer.js"},
#     {from: "API Gateway", type: "authenticates_via", to: "JWT"}
#   ]

# 5. Анализ влияния
cognee_search(
    query="что сломается если упадёт NATS",
    search_type="graph_completion"
)
# → "Все межсервисные коммуникации, включая Sites↔DataAccess"
```

---

## Как Claude автоматически использует Memory

### В начале сессии

```python
# Claude автоматически делает:
memory_search(query="контекст проекта предпочтения", user_id="current_user")
kb_search(knowledge_base="main", query="архитектура стек технологий")
```

### При обсуждении проблемы

```python
# User: "У меня баг с авторизацией"
# Claude автоматически:
memory_search(query="баги авторизация решения", user_id="team")
kb_search(knowledge_base="architecture", query="авторизация JWT токены")
```

### После решения проблемы

```python
# Claude автоматически сохраняет:
memory_add(
    content="Баг: JWT токен не обновлялся. Решение: добавить refresh в interceptor",
    user_id="team",
    metadata='{"type": "bugfix"}'
)
```

---

## Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PROJECT_ID` | Идентификатор проекта | terra |
| `COGNEE_ENABLED` | Включить Cognee | true |
| `LLM_MODEL` | LLM для Cognee | qwen3:8b |
| `OLLAMA_LLM_MODEL` | LLM для Mem0 | qwen3:8b |
| `OLLAMA_EMBEDDING_MODEL` | Модель эмбеддингов | nomic-embed-text |
| `NEO4J_AUTH` | Пароль Neo4j | neo4j/password |

### Отключение компонентов

#### Отключить LLM для Mem0

```yaml
mem0-{project}:
  environment:
    - OLLAMA_LLM_MODEL=  # Пустое значение
```

| Что теряете | Что остаётся |
|-------------|--------------|
| ❌ Автоизвлечение фактов | ✅ Базовое хранение |
| ❌ Дедупликация | ✅ Семантический поиск |
| ❌ Умное обновление | ✅ CRUD операции |

**Когда отключать:** Экономия RAM/CPU, достаточно простого хранения.

#### Отключить Cognee

```yaml
unified-{project}:
  environment:
    - COGNEE_ENABLED=false
```

| Что теряете | Что остаётся |
|-------------|--------------|
| ❌ Граф знаний | ✅ Memory (Mem0) |
| ❌ Сущности и связи | ✅ Knowledge Base |
| ❌ cognee_* инструменты | ✅ memory_* и kb_* |

**Когда отключать:** Слабый сервер, не нужен граф.

#### Минимальная конфигурация

```yaml
mem0-{project}:
  deploy:
    replicas: 0

unified-{project}:
  environment:
    - COGNEE_ENABLED=false
```

| Что теряете | Что остаётся |
|-------------|--------------|
| ❌ Память между сессиями | ✅ Knowledge Base |
| ❌ User isolation | ✅ Документация |
| ❌ Автофакты | ✅ Семантический поиск |

**Требования:** ~1 GB RAM

### Требования к серверу

| Конфигурация | CPU | RAM | Что работает |
|--------------|-----|-----|--------------|
| **Минимум** | 2 vCPU | 4 GB | Только KB |
| **Стандарт** | 4 vCPU | 8 GB | Memory + KB |
| **Полная** | 4+ dedicated | 16 GB | Memory + KB + Cognee |

> **Важно:** Cognee требует **выделенных CPU** (dedicated). На vCPU будет очень медленно (минуты на запрос).

---

## API

### Health Check

```bash
curl http://server/{project}/health
```

```json
{
  "status": "healthy",
  "service": "unified-{project}",
  "project_id": "{project}",
  "cognee_enabled": true,
  "isolation": {
    "memory": "per_user",
    "knowledge_base": "shared_per_project",
    "cognee": "shared_per_project"
  }
}
```

---

## Troubleshooting

### Cognee возвращает validation errors

**Причина:** Маленькая модель (gemma3:1b) не понимает инструкции.

**Решение:**
```yaml
environment:
  - LLM_MODEL=qwen3:8b  # Минимум 8B параметров
```

### Memory не находит записи

1. Проверить `user_id` совпадает при add и search
2. Добавить более содержательный текст (факты!)
3. Mem0 извлекает только факты — "ок" не сохранит

### 502 Bad Gateway

```bash
docker exec mcp-nginx nginx -s reload
```

### Cognee граф пустой

Граф создаётся при первом `cognee_add()`:
```python
cognee_add(content="Initial content...", source="init")
cognee_graph_stats()  # Теперь покажет данные
```

---

## Структура проекта

```
mcp-team-memory/
├── docker-compose.yml      # Основная конфигурация
├── .env                    # Переменные окружения
├── nginx/                  # Nginx + API ключи
│   ├── nginx.conf
│   └── api-keys-{project}.conf
├── unified-mcp/            # Unified MCP сервер
│   ├── main.py             # FastMCP: Memory + KB + Cognee
│   ├── requirements.txt
│   └── Dockerfile
├── mem0-src/               # Mem0 форк с Ollama
├── gateway/                # Legacy Gateway (deprecated)
├── knowledge-base/         # Файлы KB (volume)
└── init-scripts/           # Скрипты инициализации
```

---

## Лицензия

MIT
