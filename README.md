# MCP Team Memory Server v3

Self-hosted платформа долгосрочной памяти для AI-ассистентов (Claude Code, Cursor, GPT) с поддержкой множества проектов и пользователей.

## Что это?

**MCP Team Memory** позволяет AI-ассистентам помнить информацию между сессиями. Вместо того чтобы каждый раз объяснять контекст проекта, предпочтения, решённые проблемы — ассистент запоминает это навсегда.

---

## Архитектура v3 — Cipher-Centric

### Ключевые изменения в v3

| Было (v2) | Стало (v3) |
|-----------|------------|
| Mem0 + Neo4j | **Cipher** (unified memory layer) |
| Neo4j для графов | Qdrant + PostgreSQL |
| Отдельные mem0-{project} контейнеры | Единый mcp-cipher |
| 47 инструментов | Оптимизированный набор через Cipher |

### Компоненты системы

```
┌─────────────────────────────────────────────────────────────────┐
│                     v3 Architecture                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Nginx API Gateway                      │   │
│  │           /terra/mcp  /sport/mcp  /datashowcase/mcp      │   │
│  │                    API Key authentication                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│         ┌────────────────────┼────────────────────┐             │
│         ▼                    ▼                    ▼             │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │unified-terra │    │unified-sport │    │unified-      │      │
│  │  FastMCP     │    │  FastMCP     │    │datashowcase  │      │
│  │  Port 8001   │    │  Port 8002   │    │  Port 8003   │      │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘      │
│         │                   │                   │               │
│         └───────────────────┼───────────────────┘               │
│                             ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                      Cipher                               │   │
│  │            Unified Memory Layer (Node.js)                 │   │
│  │                     Port 3000                             │   │
│  │                                                           │   │
│  │  • Memory CRUD operations                                 │   │
│  │  • Semantic search                                        │   │
│  │  • MCP protocol support                                   │   │
│  │  • 5-layer deduplication                                  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                    │                    │                        │
│         ┌─────────┴────────┐   ┌───────┴────────┐               │
│         ▼                  ▼   ▼                ▼               │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐        │
│  │   Qdrant     │   │  PostgreSQL  │   │   Ollama     │        │
│  │  Vector DB   │   │  Persistence │   │  Embeddings  │        │
│  │  Port 6333   │   │  Port 5432   │   │  Port 11434  │        │
│  └──────────────┘   └──────────────┘   └──────────────┘        │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                     Graphiti                              │   │
│  │           Temporal Knowledge Graph (Python)               │   │
│  │                     Port 8084                             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             │                                    │
│                             ▼                                    │
│                    ┌──────────────┐                             │
│                    │   FalkorDB   │                             │
│                    │  Graph Store │                             │
│                    │  Port 6379   │                             │
│                    └──────────────┘                             │
└─────────────────────────────────────────────────────────────────┘
```

### Инфраструктурные сервисы

| Компонент | Технология | Назначение | Порт |
|-----------|------------|------------|------|
| **mcp-cipher** | Node.js | Центральный memory layer | 3000 |
| **mcp-qdrant** | Qdrant | Vector store для Cipher | 6333 |
| **mcp-postgres** | PostgreSQL + pgvector | Персистентность | 5432 |
| **mcp-ollama** | Ollama | Локальные embeddings | 11434 |
| **mcp-falkordb** | FalkorDB | Graph DB для Graphiti | 6379 |
| **graphiti** | Python + FastAPI | Temporal Knowledge Graph | 8084 |
| **mcp-nginx** | Nginx | API Gateway, Auth | 80 |

### Per-Project сервисы

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **unified-{project}** | FastMCP (Python) | MCP Server с Cipher интеграцией |

---

## Типы памяти

### 🧠 Cipher Memory (основной)

Cipher объединяет функционал Mem0 + SSOT в единый слой:

- **Автоматическая дедупликация** — 5-уровневый алгоритм
- **Семантический поиск** — через Qdrant vector store
- **Персистентность** — PostgreSQL backend
- **MCP протокол** — нативная поддержка

```python
# Через Unified MCP Server
mem0_add(content="Используем PostgreSQL RLS для изоляции тенантов", user_id="dev1")
mem0_search(query="изоляция данных", limit=5)
```

### 🔗 Graphiti (Temporal Knowledge Graph)

Граф сущностей и связей с историей изменений:

```python
graphiti_add_episode(content="AuthService зависит от JWTValidator", name="deps")
graphiti_search_facts(query="зависимости AuthService")
```

### 📚 Knowledge Base

Семантический поиск по документации (FAISS + Ollama):

```python
kb_create(name="architecture", documents=["# Auth Module..."])
kb_search(name="architecture", query="авторизация")
```

---

## Мультипроекты

### Текущие проекты

| Проект | Endpoint | Unified Port |
|--------|----------|--------------|
| **terra** | `/terra/mcp?key=...` | 8001 |
| **sport** | `/sport/mcp?key=...` | 8002 |
| **datashowcase** | `/datashowcase/mcp?key=...` | 8003 |
| **trialprj** | `/trialprj/mcp?key=...` | 8004 |

### Изоляция данных

- **Cipher**: Per-project через project_id parameter
- **Graphiti**: Per-project через group_id
- **Knowledge Base**: Per-project namespacing

---

## Быстрый старт

### Подключение к Claude Code

`.mcp.json` в корне проекта:

```json
{
  "mcpServers": {
    "team-memory": {
      "type": "sse",
      "url": "http://46.149.66.107/datashowcase/mcp?key=YOUR_API_KEY"
    }
  }
}
```

### Проверка подключения

```bash
# Health check (через nginx)
curl "http://46.149.66.107/terra/mcp?key=terra_user00_..." \
  -H "Accept: text/event-stream"

# Direct health (внутри сети)
curl http://localhost:8001/health
```

---

## Инфраструктура

### Ресурсы

| Компонент | Memory |
|-----------|--------|
| mcp-nginx | 64MB |
| unified-{project} × 4 | 256MB × 4 = 1GB |
| mcp-cipher | 512MB |
| mcp-qdrant | 512MB |
| mcp-postgres | 512MB |
| mcp-ollama | 4GB |
| graphiti | 768MB |
| mcp-falkordb | 512MB |
| **Total** | ~8GB |

### Управление

```bash
# Статус
docker compose ps

# Логи
docker logs unified-terra --tail 50
docker logs mcp-cipher --tail 50

# Перезапуск
docker compose restart unified-terra
docker compose restart mcp-cipher
```

---

## Changelog

### v3.0.0 (2025-12-06) — Cipher-Centric Architecture

- 🔄 **Breaking**: Замена Mem0 + Neo4j на Cipher
- ✅ Cipher как центральный memory layer
- ✅ Qdrant vector store
- ✅ Упрощённая архитектура (меньше контейнеров)
- ✅ Unified MCP серверы с Cipher интеграцией
- ✅ Сохранена совместимость mem0_* API

### v2.0.0 (2025-12-06) — SSOT Architecture

- ✅ PostgreSQL SSOT layer
- ✅ 5-уровневая дедупликация
- ✅ Unified search view

### v1.0.0 (2025-12-05) — Initial Release

- ✅ Mem0 + Anthropic Claude
- ✅ Graphiti temporal graph
- ✅ Knowledge Base с FAISS

---

## Лицензия

MIT
